{
  "name": "safracontrol-integration-docs",
  "version": "1.0.0",
  "description": "Documentação de integração da API SafraControl",
  "scripts": {
    "dev": "npx redocly preview-docs https://safracontrol.api.stg.services.souagrosolucoes.com.br/integration/api-json",
    "lint": "npx redocly lint",
    "build": "mkdir -p dist && cp index.html dist/index.html && cp -r docs dist/docs"
  },
  "devDependencies": {
    "@redocly/cli": "^1.0.0"
  }
}
