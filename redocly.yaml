apis:
  main:
    root: https://safracontrol.api.stg.services.souagrosolucoes.com.br/integration/api-json

theme:
  openapi:
    theme:
      colors:
        primary:
          main: "#0066cc"
      typography:
        fontFamily: "Roboto, sans-serif"
        headingsFamily: "Montserrat, sans-serif"

lint:
  rules:
    no-unused-components: warn
    operation-summary: error
    info-contact: warn
